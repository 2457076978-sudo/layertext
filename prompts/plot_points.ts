/** 原 prompts/plot_points.md，2026-09-16 内联为 TS 常量（vite `?raw` 同一字符串，行为零变化）。
 *  改提示词流程不变：改这里 + prompts/manifest.json 版本号 + CHANGELOG（见 prompts/README.md）。 */
export const promptPlotPoints = `阅读下面的英语章节，站在英语教师改编分层简化版的立场，摘出"简化时绝对不能丢的情节点与伏笔"。
要求：
- 5~8 条，每条不超过 20 个字，用中文；
- 只写情节与伏笔（人物、事件、因果、转折、后文伏笔），不写语言难度评价；
- 你的结果是初步候选：教师会逐条勾选确认，不确定的宁可不写；
- 输出：只输出一个 JSON 字符串数组，形如 ["要点一","要点二"]，不要任何其他文字。

章节正文：
{{chapter}}
`;
