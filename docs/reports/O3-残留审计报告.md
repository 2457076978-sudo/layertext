# O3 文案与残留审计报告

> 2026-09-07 · 优化阶段 O3。范围：产品转向（去预设难度）后，全库用户可见文案中的"难度层/分层"残留清理 + 欠账 #3（复盘页归属）结论。

## 一、审计方法

对 `app/src/**`、`app/index.html`、`app/public/help*.html`、`app/src-tauri/src/main.rs`、`README.md`、`docs/PRD.md`、`prompts/**` 全文 grep：
`难度层 / 分层方案 / B层 / M层 / A层 / 分层初稿 / 解禁 / 三层 / 层级 / 中梯 / 支架 / 挑战`。
引擎内部（`src/core/`）三层口径**不在审计范围**（红线：保留为引擎能力，应用统一以 M 口径调用）。

## 二、修复清单（用户可见文案）

| # | 位置 | 原文 | 改为 | 依据 |
|---|------|------|------|------|
| 1 | 质检报告表格（main.ts） | 表头「解禁门（A层）」 | 「句法黑名单」 | A 层解禁逻辑已随产品转向移除 |
| 2 | 书级规则检查提示（main.ts） | 建议用「分层初稿」按规则重写 | 「📖 整章改写」 | 功能 2026-09-06 已改名，旧名指向不存在的入口 |
| 3 | 首启动向导·AI 步（main.ts） | AI 负责"帮改写"：分层初稿、逐句改写建议、对话助手 | 整章改写、按标记修改、对话助手 | 同上，且与工具栏现行命名对齐 |
| 4 | 首启动向导·欢迎页（main.ts） | 把英文原著**简化成不同难度的版本**给学生读 | **简化成学生能读的版本**（简化到什么程度由你的词库决定） | "多个难度版本"是旧三层模型的核心话术 |
| 5 | 终审门禁 QC 说明（types.ts） | 达到**本层级**参考标准 | 达到简化标准（句长上限可调） | 无层级概念 |
| 6 | 门禁帮助核对表脚注（main.ts） | 参考值源自原型项目三层设计 | 参考值即当前简化标准（句长上限 ⓘ 可调） | 原型背景不应对教师暴露 |
| 7 | PRD §1 | 改编成多个难度层 | 简化成学生能读的版本 | 用户定义与现行产品一致 |
| 8 | prompts/README.md | system_draft = 分层初稿 system | 整章简化 system | 与 manifest desc 对齐 |
| 9 | index.html CSS 注释 ×2 | 分层初稿弹层 / 分层方案&欢迎弹层 | 整章改写弹层 / 简化标准&欢迎弹层 | 注释级，顺手清理 |

## 三、保留项（有意不动）

- **产品名「LayerText 分层读」**：品牌名，帮助页/关于框保留。
- **README 方法论段"源自一个真实项目（英文原著三难度层全书调适）"**：历史起源的事实陈述（路线图区已有转向记录），非产品承诺，保留。
- **main.ts `tagFromPath` 的 'A层' 路径识别**：兼容 AF 原型遗留文件的报告命名（`质检报告_A层.json`），纯内部逻辑、不可见，保留。
- **"本工具不预设难度层"（简化标准弹层）**：新模型的解释性话术，正是要说"没有预设层"，保留。
- **引擎内部三层口径（src/core/、tools/qc_chapter_ref.py）**：红线，不动。

## 四、中梯 / 支架 / 挑战

全库零命中，无需处理。

## 五、欠账 #3 关闭：「复盘」标签页归属确认

**结论：归属明确、无需合并去重，欠账关闭。**

- git 考古（`git log -S 复盘 -- app/src/main.ts`）：复盘页代码仅来自两笔工程化提交——09ddcb7（W2 采纳率数据闭环，2026-09-06 新增"应用内复盘页：视图标签+显示菜单"）与 2c6c70d（W3 补充"本书 AI 成本卡片"）。**不是来源不明的改动**，是 W2/W3 的正式交付；此前"界面出现来源不明 tab"的疑虑来自两个会话并行开发时信息不同步。
- 功能分工清晰无重叠：「复盘」页 = AI 建议台账的聚合看板（采纳率/成本/趋势，读 CSV 自动生成）；「修订建议」页 = 当前会话逐条 AI 建议的勾选采纳表（流程产物）。一个看历史数据、一个处理当下建议。
- 后续仅做一处对齐：两页在界面上的入口（视图标签栏相邻）语义已通过各自空状态文案说明，无教师反馈混淆，不再改动。

## 六、验证

- `npm test` 83 项全绿；app `tsc --noEmit` 通过；`vite build` 通过。
- 逐屏复核：工具栏、诊断台、正文、门禁帮助、首启动向导、简化标准弹层、书级规则弹层、全书批处理弹层（本次新增）——已无指向旧模型的措辞。
