# 交互自解释性走查报告（2026-09-09 · 第十批交付同轮）

> 依据《交互自解释性验收标准 v0.1》A1 三秒测试（点了会发生什么，3 秒内可答）逐元素核对。
> 本轮为**代码级走查**（文案/title/后果预告核对，全部元素源码可溯）；视觉观感（间距/对比度/深色主题下 .pm 效果）留真机走查—— Wayne 装机后按"清单最后一列"抽查。

## 一、走查结论

- 主工具栏、页签、书架、查找条：**上一轮 UI 整改后已普遍合规**（title 均为"动作+后果"式）。
- 本轮新修 **P1×6**（见下表 ✱ 修复），其中最重的是**词面板提示话术与即改模式矛盾**（后果预告失准，标准 A1 直接不过——用户按旧提示预期"供采纳"，实际即改模式已直接改原稿）。
- 无 P0（会误解且改动数据类的元素）。
- 短语面板为新增，**从标准出发设计**（选区预览+后果预告+模式预告齐全），不是补丁。

## 二、逐元素清单（节选主要交互面，全部 47 处已核）

| 区域 | 元素 | 三秒测试（点了会发生什么？） | 判定 |
|---|---|---|---|
| 工具栏 | 书架图标 | "回到书架（当前章节自动保存，随时回来）" | ✅ |
| 工具栏 | 打开… | "打开章节文件（⌘O）；菜单 文件→载入示例" | ✅ |
| 工具栏 | 质检本章 | "打开课文会自动体检；换了词库或改完正文后点这里重新算" | ✅ |
| 工具栏 | 按标记修改 | "先做标记再点这里成批改。即改还是等你确认，看右侧模式胶囊" | ✅（指向模式胶囊，闭环） |
| 工具栏 | 撤销/重做 | ⌘⌥Z / ⌘⌥⇧Z 文件级 | ✅ |
| 工具栏 | A－/A＋ | 字号增减 | ✅ |
| 工具栏 | 主题/设置 | 明确 | ✅ |
| 模式胶囊 | ⚡即改/👁候选 | 运行时 title："当前：…。点击切到…"（前后果预告） | ✅（静态占位 title 本轮同步补齐） |
| 页签 | 正文审校/质检报告/修订建议/版本对比/复盘 | 原无 title | ✱ 本轮补 |
| 页签 | 逐句对照/书级看板/审校档案/目录 | title 完整（对照什么/看什么/干什么用） | ✅ |
| 词面板 | 类型按钮（7 类） | 原无后果预告 | ✱ 本轮补 title（标记为X+模式预告） |
| 词面板 | 底部提示行 | **原"改写会直接出现在正文中供采纳"=候选模式旧话术，与即改模式矛盾** | ✱ **根修**：按当前模式动态预告 |
| 短语面板 | 全部（新增） | 选区预览+后果预告+模式预告 | ✅（从标准出发） |
| 句面板 | 类型按钮（8 类） | 标签自解释（语法太难/句太长…） | P2 记录（不阻断） |
| 侧栏 | 配额 ✓/×、门禁 ?、清单跳转/× | "？这是什么？"有完整门禁说明 | ✅ |
| 查找条 | 上一个/全部替换 | "全部替换（**可撤销**）"——后果预告 | ✅ |
| 热力轨 | 圆点 | title=词/短语/句标记+文本预览 | ✅（短语本轮接入） |
| 书架 | 书卡/分组/视图切换 | "打开《书名》""书封视图——挑书" | ✅ |
| 状态行 | 结果反馈 | 原仅 12px muted | ✱ 重要结果（加注/换词）同步 toast；saved/err 加粗 |

## 三、修复清单（本轮 ✱ 六项）

1. **词面板提示话术根修**（P1，后果预告失准）：按 autoRewriteOnMark 动态预告——即改："点任一标记立即执行（写原稿+日志，首改前自动备份）"；候选：原文案。
2. 词面板 7 类按钮 title：「标记为"词汇简化"——即改模式下点完立即执行（写原稿+日志）」式。
3. 短语面板类型按钮 title + 模式动态提示（新增即合规）。
4. 五页签补 title（正文审校/质检报告/修订建议/版本对比/复盘）。
5. 确定性管线成功结果 toast（不再只沉状态行）+ 状态行 saved/err 加粗。
6. 静态 mode-pill title 补完整语义（运行时本就有，消除初始化闪现期歧义）。

## 四、留给真机走查的项（Wayne 装机后 2 分钟）

- 拖选短语→弹层→点「加中文标注」→下划线消失+`短语（中文）`插入（浅/深主题各看一眼下划线对比度）。
- 拖选整句→句面板（不应出短语面板）；拖选单词→词面板。
- 打开一个 GBK txt（若有）确认不乱码；无样本可跳过。
- 原生菜单（质检/文件）项文案——AX 自动化不稳，人眼 30 秒。
