# AGENTS.md — 在本仓库动手之前先读这一页

> 这一页只写**每次动手都要遵守的约定**，不写教程。
> 想知道"东西在哪、该改哪儿"→ [`docs/文件架构.md`](docs/文件架构.md)（架构地图）。
> 想知道"这个机制为什么长这样"→ [`CHANGELOG.md`](CHANGELOG.md) 与 `docs/`。

---

## 一、三条铁律

### 1. 改结构，必须同步改地图（同一个提交里）

**任何改动目录结构、新增/删除/移动文件、或改变文件职责的改动，
必须同步更新 [`docs/文件架构.md`](docs/文件架构.md)，并在它的
「本文件自己的变更记录」里加一行。**

`npm run preflight` 会拦最粗的失效（地图被删 / README 没链到），但**拦不住"改了不写"**——
那一条靠你。理由写在架构地图开头：**过时的地图比没有地图更坏**。

### 2. 每次改动都要写 CHANGELOG

不是"重要改动"才写。**每一次都写**：改了什么、为什么、怎么验证的。
如实记录失败与返工（例：某次合并用 `checkout --ours` 整文件取边，丢了另一侧的非冲突改动，
靠 `USER=runner` 复现 CI 条件才抓到）。

### 3. 教师自己的材料，一律不进仓库

仓库是公开的（source-available）。**不进仓库**的东西：

- 书稿正文、三层产物、教师词库/词典/专名表/知识库
- `app/public/_fixture/`（UI 走查夹具）、`tests/fixtures/replay/`（回放夹具）、`tools/_af_*`
- 学生数据：成绩、班级画像、学号、名册——**碰它们之前先读 `docs/数据红线与使用者须知.md`**
- 本机绝对路径（`/Users/...`）不要写进代码/测试/文档；需要真实路径时**从环境变量取，取不到就跳过**
  （成例：`LAYERTEXT_AF_DIR`、`LAYERTEXT_REPLAY_DIR`）

**发布包里也不许有学生数据**——`src/core/bundle.ts` 有白名单 + 黑名单双重拦截，
改动它就是在改一道数据红线，别顺手放松。

---

## 二、开工前 30 秒

```bash
npm run verify      # preflight + typecheck + lint + test（CI 跑的就是它）
```

改完再跑一次。**CI 上的 `USER=runner` 是本机默认复现不出来的**——涉及"跑真脚本"的测试，
本地用下面这条自测，历史上整整一轮 CI 只因为这个变量假红：

```bash
USER=runner npm run verify
```

---

## 三、这个仓库的物理形态（最容易踩的一条）

**两个 worktree，同一份 `.git`**：

| 路径                       | 分支                       | 是什么                 |
| -------------------------- | -------------------------- | ---------------------- |
| `…/LayerText`              | `main`                     | 主仓                   |
| `…/LayerText-optimization` | `codex/total-optimization` | 特性分支（App 开发位） |

**项目配置里的「引擎目录」指向主仓**，管线脚本 import 的是它的 `dist/`。
所以 **`src/`、`tools/` 的改动要两边都落**，只改一边等于没改。

推送命令、网络绕行与凭据见 `docs/文件架构.md` 第十节。

---

## 四、写代码时的几条既有口径（违反会被测试或评审拦下）

- **同一条判定只许有一份实现**：`app/` 与 `tools/af_pipeline/` 都从 `src/core/` 取，不许各写一套。
- **不许静默吞错**：每个 `catch` 要么说出口（`setStatus` / `toast` / 状态行）、要么抛、
  要么显式返回失败、要么写明 `有意兜底：<理由>`。`tests/appswallow.test.ts` 会扫。
- **事件是正本，视图可重建**：台账/决定用 append-only JSONL，标记文件只是视图。
- **重算不许抹掉人的决定**：队列/台账重跑时，教师点过的状态要按稳定 ID 带回来。
- **人定的东西不能被机器覆盖**：教师指定的内容不送进 AI，冲突时以人为准。
- **判定用数据定线，不拍脑袋**：阈值要写清它从哪来、什么条件下失效。
- **响应式纯逻辑优先**：能写成纯函数 + 单测的，不许塞进 DOM/Tauri 依赖里。

---

## 五、提交

- 提交信息写**为什么**，不只是做了什么；如实标注失败、返工、没做到的部分。
- 提交前 `npm run verify` 全绿（husky 会跑 lint-staged：eslint --fix + prettier --write）。
- **架构/位置变了** → 提交里同时有 `docs/文件架构.md` 的改动（见铁律 1）。
